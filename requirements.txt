# ser2net — runtime dependencies (pinned). All wheels are bundled in vendor/wheels/
# for fully offline installation (see bootstrap.py).
#
# Pure-Python (work on every OS/Python):
pyserial==3.5
pyserial-asyncio-fast==0.16
starlette==1.2.1
uvicorn==0.49.0
websockets==16.0
python-multipart==0.0.32
jinja2==3.1.6
anyio==4.13.0
click==8.4.1
h11==0.16.0
idna==3.18
typing_extensions==4.15.0
# Python 3.10 backports (pure-Python, only installed on < 3.11). anyio needs
# exceptiongroup there; ser2net additionally uses exceptiongroup + taskgroup as
# backports of asyncio.TaskGroup / asyncio.Runner / except* so app/engine/bridge.py
# and app/runtime.py run on 3.10. On 3.11+ these are native and not installed.
exceptiongroup==1.3.1 ; python_version < "3.11"
taskgroup==0.2.2 ; python_version < "3.11"
# click pulls in colorama on Windows (for ANSI colors) on every Python version.
colorama==0.4.6 ; platform_system == "Windows"
# Platform-specific (win_amd64 + manylinux wheels bundled for cp311/cp312).
# markupsafe also ships a pure-Python py3-none-any wheel here so it installs
# offline on ANY Python (e.g. 3.10/3.13/3.14): without a matching _speedups
# .pyd it falls back to its native Python escaper. psutil's cp3x-abi3 wheel
# already covers Python 3.7+.
markupsafe==3.0.3
psutil==7.2.2

# --- Optional, NOT required (graceful fallback if absent) ---
# pyudev>=0.24      ; sys_platform == "linux"   # event-driven hotplug on Linux
# pywin32>=306      ; sys_platform == "win32"    # event-driven hotplug on Windows
# paho-mqtt>=1.6                                 # per-mapping MQTT publishing (serial->broker)
# ldap3>=2.9                                      # LDAP / Active Directory authentication
# authlib>=1.3                                    # OIDC single sign-on (id_token validation)
